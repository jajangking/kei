name: Build Firmware

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
          key: pio-${{ runner.os }}

      - name: Install PlatformIO
        run: pip install platformio

      - name: Build firmware
        run: set -o pipefail; pio run -d esp32 -e esp32dev 2>&1 | tee /tmp/pio_output.txt

      - name: Upload error log if failed
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: /tmp/pio_output.txt

      - name: Stage binary
        if: success()
        run: |
          mkdir -p /tmp/release
          cp esp32/.pio/build/esp32dev/firmware.bin /tmp/release/firmware.bin
          ls -la /tmp/release/

      - name: Upload binary
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: /tmp/release/firmware.bin

      - name: Create Release
        if: success() && github.ref == 'refs/heads/main'
        run: |
          gh release create "build-${{ github.run_number }}" \
            /tmp/release/firmware.bin \
            --repo "${{ github.repository }}" \
            --title "Firmware Build #${{ github.run_number }}" \
            --notes "Flash ke 0x10000:  esptool.py write_flash 0x10000 firmware.bin"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}